(function() { try { const selector = '{{selector}}'; const element = document.querySelector(selector); if (!element) { return { success: false, error: 'Element not found with selector: ' + selector }; } if (typeof element.click === 'function') { element.click(); return { success: true, message: 'Clicked element: ' + selector, tagName: element.tagName, text: (element.textContent || '').trim().substring(0, 100) }; } else { return { success: false, error: 'Element is not clickable: ' + selector }; } } catch(e) { return { success: false, error: 'Failed to click: ' + (e.message || String(e)) }; } })()

